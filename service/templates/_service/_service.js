/**
 * <%= appName %>
 * @module service.<%= serviceName %>
 */
angular.module('service.<%= serviceName %>', [])
    .factory('<%= serviceName %>', function () {
        'use strict';

        // The service logic goes here...
    });
